"""宽基四腿的买卖闸倍数——**全系统唯一定义处**。

生产提示行（`orchestration/action_plan.py`）与回测引擎（`scripts/analysis/long_window_backtest.py`）
都从这里取值。本模块刻意不 import 任何东西，回测脚本可以零代价引用。

## 为什么要有这个文件（P58，2026-08-05）

在此之前两边各写各的，且**写的不是同一个数**：

| 腿 | 生产提示行（旧） | 回测引擎 |
|---|---|---|
| 沪深300 | ×1.00 | ×1.30 |
| 创业板 | ×1.10 | ×1.43 |
| 科创50 | ×1.30 | ×1.30 ✅ |
| 红利 | ×1.00 | ×1.30 |

⟹ **被回测验证的策略，和每天提示给 owner 的策略，不是同一套**，三条腿的卖出线差 30%。
根因：P31 于 2026-08-02 被 E30 红队推翻回滚时只改了 `_p31_sell_hint`，没同步 `_BROAD_LEGS`，
残留下来的 1.00/1.10 从此**没有被任何人选过、也没过任何判据**。

## owner 2026-08-05 拍板：统一到 1.30（创业板 1.43）

依据三条：①四腿年化全高、夏普不劣（0.32/0.69/0.53/0.37 vs 0.31/0.71/0.54/0.37 ≈ 平）；
②卖出笔数近乎减半（沪深300 19.5 年 174→76 笔）——本系统手动执行，操作负担是真实成本；
③科创50 本来就是 1.30 且是 08-02 有数据支撑的决定，统一到 1.00 反而要覆盖它。

**代价已披露**：回撤 −31.1→−33.0（沪深300）／−36.7→−43.8（创业板）／
−16.6→−20.1（科创50）／−31.2→−33.7（红利）；均仓 41%→52%。

⚠️ **记账口径：这不是「E51 通过」，是 owner 明示越过 E51。**
E51 判据①要求四腿年化提升 ≥+0.50pp（红利仅 +0.27pp）、判据②要求 |回撤| 恶化 ≤3pp
（创业板 7.1pp），两条都不过。越过的理由是 E51 的前提不成立——它按
「×1.00 是既有基线、抬闸属放松」写的，而 ×1.00 是回滚事故的残留，不是可被保护的决定。

## 为什么是 1.30 而不是别的数（`scripts/analysis/sell_gate_sweep.py`）

细网格 1.00~1.60 步长 0.025 的读数：
- 1.30 落在**平顶**上——±0.10 邻域内年化极差仅 0.25~0.35pp；
- 1.30 **不占任何一条腿的样本内峰值**（峰在 1.350／1.525／1.425／1.600）
  ⟹ 反证它不是照样本内最优拟合出来的；
- 3/4 条腿的**分半 argmax 分歧 0.20~0.43** ⟹ 那些「最优」是噪声不是结构；
- 夏普最优点普遍在 1.30 **下方**（1.25／1.075／1.175），年化最优在**上方**，1.30 夹在中间；
- 尺度：整条网格能动年化 0.95~2.81pp，而四腿在同一闸位相差 **10.78pp**
  ⟹ 闸位是二阶旋钮，**不做细调**。

改这里的值＝改生产行为，必须走治理并同步更新上面的披露。
"""

from __future__ import annotations

# 买入闸：收盘 < 中位线 × 该值（科创50 不走锚买，走深回撤阶梯，此值仅占位）
BUY_MUL: dict[str, float] = {
    "沪深300": 1.00,
    "创业板": 0.90,
    "科创50": 1.00,
    "红利": 1.00,
}

# 卖出闸：收盘 > 中位线 × 该值 → 每月末减持仓 5%
SELL_MUL: dict[str, float] = {
    "沪深300": 1.30,
    "创业板": 1.43,
    "科创50": 1.30,
    "红利": 1.30,
}

# 科创50 深回撤阶梯（P27 v2·E28 简化篮子）：距全历史峰 −50/−55/−60/−65 四档，
# 各档一轮只买一次，投当前现金 30/35/40/50%；episode 在距峰收复到 −RUNG[0]×0.5（−25%）
# 时复位重新武装。peak=cummax 自数据首日起、无预热（2026-08-04 红队 F1 修正）。
# 生产状态机（action_plan._broad_leg_states）与回测引擎（long_window_backtest）共用
# 这一份定义——2026-08-08 前生产侧该腿谓词写死 False（阶梯只存在于文案与图表里，
# 价格真跌到第一档不会触发任何东西），handoff §2.1.1 补齐。
LADDER_RUNG: tuple[float, ...] = (0.50, 0.55, 0.60, 0.65)
LADDER_FRAC: tuple[float, ...] = (0.30, 0.35, 0.40, 0.50)

# 回测引擎里腿名到本表键的映射（两边命名一致时为恒等，留作显式契约）
LEG_NAMES: tuple[str, ...] = ("沪深300", "创业板", "科创50", "红利")

assert set(BUY_MUL) == set(SELL_MUL) == set(LEG_NAMES), "四腿键必须一致"
assert len(LADDER_RUNG) == len(LADDER_FRAC), "阶梯档位与资金比例必须一一对应"
