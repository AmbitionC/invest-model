# 数据源（生产数据更新；本地合成验证不需要）
tushare>=1.2.89

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# 数据库（MySQL 生产 / SQLite 本地验证）
sqlalchemy>=2.0.0
pymysql>=1.1.0

# 配置 / 日志
pyyaml>=6.0
python-dotenv>=1.0.0
loguru>=0.7.0

# 测试
pytest>=7.4.0
