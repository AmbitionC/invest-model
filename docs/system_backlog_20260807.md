# 系统待处理事项台账（invest-model）

**建立日期**：2026-08-07（周五）　**整理范围**：`docs/review_meta_audit_202608.md`（08-02）· issue #8 的 08-01 复盘（截至 20260731）· `docs/model_change_proposals.md` 未闭合条目 · 三仓 CLAUDE.md 🔴 项 · issue #11 运行时告警 · `docs/specs/P70_*.md`
**本次只读、未改任何生产代码。** 分支 `claude/invest-model-weekly-review-6t4ktf`。

> **⏩ 2026-08-08 更新**：MS-01/MS-02/MS-03/MS-05/MS-06 及 JSON 双轨、快照日标注已由接手会话
> 落地修复（入口与明细见 `docs/handoff_20260808.md`「处理进展」）。⚠️ 本台账多处引用的
> `review_meta_audit.py` **退出码语义已翻转**：修复落地后该脚本转为回归护栏，
> **exit 0=修复在位、1=回归**（台账正文里「退出码 0=仍复现」的旧表述以脚本 docstring 为准）。

## 使用约定

- **状态**分三档：`已确认`（有脚本复现 / 读代码或报表原文即可定，无推断成分）· `疑似·待验证`（机制成立但该具体实例未直接观测）· `待 DB 确认`（本环境无 DB 凭据，附该跑的 SQL）。
- **排序按「再不处理会造成什么损失」**，不按提出时间。🔴＝正在污染决策或正在丢数据；🟠＝时钟没在走 / 判据不可满足；🟡＝已记录在案、暂无进一步损失。
- 查不到出处的条目一律不收。行号为 2026-08-07 当日 HEAD（`6f26586`）的实际行号；**元审计文档里有两处行号已过期，见 MS-01 / MS-04 的「行号更正」**。

---

## 一、台账主表

| ID | 类别 | 一句话描述 | 触发/解锁条件 | 当前状态 | 证据 | 需 owner 拍板 | 提出日期 |
|---|---|---|---|---|---|---|---|
| **RT-01** 🔴 | 运行时健康 | 17:00 出计划定时器**已连续 15 个交易日全数失效**（07-17 起每个交易日都有哨兵告警）；更严重的是 **08-03 起连续 4 个交易日 17:40 与 18:30 各告警一次**，而告警只在「当日 action_plan 零行」分支发出 ⟹ 17:40 哨兵的补跑本身也没写进计划 | 排查 FC `invest-scheduler` 定时器；给哨兵加二级兜底 | **已确认**（双告警事实 + 代码路径）；「17:40 补跑失败」为**疑似·待 DB 确认** | issue #11 comments [5164746935](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5164746935)/[5165215807](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5165215807)（08-03 双发）、[5177255051](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5177255051)/[5177786380](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5177786380)（08-04）、[5190163350](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5190163350)/[5190624938](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5190624938)（08-05）、[5202948732](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5202948732)/[5203452360](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5203452360)（08-06）；`faas/jobs.py:490-508`（`n>0` 即静默返回、告警只在 `n==0` 分支且 ETF 分支文案不同） | 是（排查/加兜底） | 08-02（元审计报 4 天）→ **08-07 复核发现已扩大到 15 天 + 哨兵自身失效** |
| **MS-01** 🔴 | 量尺缺陷 | `buy_fill_rate` 把「计划 +0 股、实际 +0 股」判成执行率 100%，买点兑现率虚高 8/9=89%，真实有效兑现 1/2=50%；**已连续两期误报**，且是「买入守纪律、卖出不守纪律」这一结论的唯一来源 | 新增 `no_op` 状态，`planned_shares=0` 不进分子分母 | **已确认**（`python scripts/analysis/review_meta_audit.py` 退出码 0，2026-08-07 复跑仍复现） | `invest_model/review/execution.py:166-167`（一手容忍带）；issue #8 [08-01 复盘](https://github.com/AmbitionC/invest-model/issues/8#issuecomment-5151100582)第六段 9 条 buy 里 7 条计划 +0 全显示 ✅已执行；`docs/review_meta_audit_202608.md:40-65`。**行号更正**：生成侧防护实为 `invest_model/orchestration/action_plan.py:1193-1198`，元审计写的 `action_plan.py:880-885` 已过期 | 是（元审计建议①，至今未采纳） | 08-02 |
| **RT-02** 🔴 | 运行时健康 | `results/review/latest.json` 停在 **20260724**，而 `docs/review_schema.md:8` 声明「固定入口，每次复盘覆盖」并给出「Agent 开仓即读、无需 DB 凭据」的读取规则 ⟹ 任何按 schema 从零上下文消费 JSON 的 Agent 会拿 8 天前数据当最新。**机制**：`scripts/review.py` 的 `--json-dir` 默认值就是 `results/review`，JSON **确实生成了**，但生成在 FC 容器的临时文件系统里，`job_weekly_rebuild_review` 从不 commit/push | FaaS 侧经 GitHub API 提交 `latest.json`，或改 `review_schema.md` 口径声明 | **已确认** | `results/review/latest.json` 的 `report_date=20260724`/`generated_at=2026-07-27`；`faas/jobs.py:793-796`（只传 `--out`，无提交步骤）；`scripts/review.py:745`（`--json-dir` 默认 `results/review`）、`:751-758`；`docs/review_schema.md:8` | 是（元审计建议③） | 08-02 |
| **DQ-01** 🔴 | 数据质量 | 持仓快照断更：近 10 交易日覆盖 **7/10 → 6/10**，缺 0721/0729/0730/0731；0728–0730 的 **6 条 sell 指令完全无法对账**（0→6 条）。#263 安全修复把快照 CSV 移出版本控制、停用 git push 入库路径，**补录门槛同时被抬高** | 恢复快照入库（FaaS API 或本地脚本直连 DB）并补录缺口日 | **已确认**（08-01 报表原文） | issue #8 [08-01 复盘](https://github.com/AmbitionC/invest-model/issues/8#issuecomment-5151100582)第六段「近10交易日快照覆盖 6/10，缺 20260721、20260729、20260730、20260731」+ 6 行「—无法对账·缺快照」；`docs/review_meta_audit_202608.md:178-191` | 是（补录路径需决定走哪条） | 08-02 |
| **MS-02** 🔴 | 量尺缺陷 | 强风控告警的**熄火由价格驱动、不由执行行为驱动**：`condition_still_valid = last_close <= stop_price*1.02`，价格回到止损位上方 2% 即告警静默、未执行成本转正。⟹ **一笔拖着不执行的强风控单，只要等到一次反弹就自动从复盘里消失、计数归零**（卫星化学 0721 那条 07-27 还告警、08-01 消失） | 降级为「曾触发·现已回落」历史行，保留累计计数 | **已确认**（脚本复现，机制层）；「卫星化学本例确由此熄火」为**疑似·待验证**（该标的 `stop_price` 本地不可观测） | `invest_model/review/execution.py:184`；`scripts/review.py:489`、`:704`（告警四条件 AND 含 `condition_still_valid`）；`docs/review_meta_audit_202608.md:69-91` | 是（元审计建议④） | 08-02 |
| **MS-06** 🔴 | 量尺缺陷 | **账户最大的单点风险在校准系统里完全不可见**：08-01 快照卫星化学 165,088 / 持仓合计 255,041 = **单票 64.7%**、贡献 −67% 浮亏，该票自 0703 起被计划卖出 **6 次**（0703/0707/0708/0717/0721/0724）全部未执行；复盘六段无任何集中度指标 | 第三段补集中度行（最大单票 / 前三大占比） | **已确认**（`grep 集中度\|concentration\|top1 scripts/review.py` 零命中） | issue #8 [08-01 复盘](https://github.com/AmbitionC/invest-model/issues/8#issuecomment-5151100582)第三段与第六段；`docs/review_meta_audit_202608.md:194-199` | 是（元审计建议⑤） | 08-02 |
| **MS-05** 🔴 | 量尺缺陷 | **复盘六段无一覆盖本轮上线的提示层能力**：`grep "broad\|leverage\|宽基\|P26\|P30" scripts/review.py` **零命中**；第五段 sleeve 仍是旧套利 sleeve（alpha/defense_A/offense_B）。治理后果：**P30 后置义务白纸黑字写「触发记复盘」，但复盘里没有承接它的段落**，P26/P27/P28 同样无状态回溯 ⟹ 提示层上线即失访 | 新增第七段：宽基四腿窗口 / P26 中位线位置 / 恐慌值 / P28·P30 信号状态与触发史 | **已确认**（2026-08-07 复跑 grep 仍零命中） | `scripts/review.py`（grep 零命中）；`docs/model_change_proposals.md:627`（P30「后置义务＝每次触发记录进复盘」）；`docs/review_meta_audit_202608.md:151-161` | 是（元审计建议⑥） | 08-02 |
| **MS-03** 🟠 | 量尺缺陷 | 分级表**两列给出相反排序且当前无法裁定**：至今超额 C>A>B、10 日窗口 A>B>C；复盘 A1 修订原话是「校准分级权重**优先看 10 日列**」，而 **P23 的证据①采信的正是被标注为「仅作参考」的至今列**。更要命的是 `by_grade[]` 只有全样本 `n`，**`mean_ret_h10`/`win_rate_h10` 没有对应样本量字段** ⟹ 两列矛盾时无从判断谁可信 | 分级表与 JSON 补 `n_h10` 样本量 | **已确认**（`grep n_h10 scripts/review.py` 零命中） | `scripts/review.py:193-204`（`"n": int(len(sub2))` 为全样本；`mean_ret_h10` 用 `sub2["ret_h"].dropna()` 但长度未落字段）；issue #8 [08-01 复盘](https://github.com/AmbitionC/invest-model/issues/8#issuecomment-5151100582)第一段表；`docs/review_meta_audit_202608.md:94-114` | 是（元审计建议②） | 08-02 |
| **MS-04** 🟠 | 量尺缺陷 | 影子对账 **−23.0pp 测的可能不是筛选能力而是空仓 beta 差**：`gate_all` 把未触发信号按 0 计入均值，而 **73 条里 72 条从未触发**；由均值 −0.3% 反推唯一出手那笔 ∈ [−25.6%, −18.3%)，而速通均值 −23.2% 正落在该区间内。且 `fast_ret`/`gate_ret` 是「至今」累计口径、无固定窗口，与被判「仅作参考」的至今列同源缺陷。若成立，**E18 判据测的是单边下跌里空仓 vs 满仓的 beta 差** | 登记 E18 v2 并列口径（窗口改 10 日固定；未触发信号剔出分母或改用同期沪深300 作空仓机会成本），首评时两版并读 | **已确认**（代码 + 报表算术）＋ **疑似·待验证**（「闸门无筛选能力」这一解释 n=1，不足以定论） | `scripts/review.py:531`（`gate_all = pd.concat([gate_hit, pd.Series([0.0]*n_gate_miss)])`）；issue #8 [08-01 复盘](https://github.com/AmbitionC/invest-model/issues/8#issuecomment-5151100582)「四·附」段；`docs/review_meta_audit_202608.md:118-134`。**行号更正**：`fast_ret`/`gate_ret` 实为 `invest_model/orchestration/action_plan.py:852-853`，元审计写的 `action_plan.py:538-539` 已过期 | 是（元审计建议⑦，**只改判据登记方式、不改判据数值**） | 08-02 |
| **OB-03** 🟠 | 后置义务 | **E18 的裁决门槛在当前信号结构下无到达日期**：影子样本 73 条、两期零增长（07-24→07-31 逐字不变）；影子只收 research 级 A/B，而仓内含 `research` 的个股信号文件最后一份是 `config/advisor_signals_20260712_weekend.csv`——**已断流约 4 周**；其后入库的 0731 两份全是 `intraday`+`C` 级不进影子 ⟹ **P23 被无限期悬置：既不执行也不撤销** | E18 加时间兜底「≥100 条**或**登记满 90 天，取先到者首评」（判据数值不动）；或恢复 research 级投顾输出 | **已确认** | `docs/model_change_proposals.md:519`（E18「影子样本 ≥100 条时首评」）；`config/advisor_signals_20260712_weekend.csv` 为最后一份 research；`docs/review_meta_audit_202608.md:138-148` | 是（元审计建议⑧） | 08-02 |
| **OB-05** 🟠 | 后置义务 | **E47 的「数据层连续入库 ≥6 个月」时钟根本没在走**：`ingest-macro.yml` **只有 `workflow_dispatch` 与 trigger 文件 push 两个触发器、无任何 `schedule`/cron**；`ops/ingest-macro.trigger` 自 2026-08-05 建仓以来只有过一次值（`1`）⟹ 宏观数据不是「连续入库」而是「入库过一次」，等到 2027-02 首评时会发现 6 个月的连续序列并不存在 | 给 ingest-macro 加月频 cron（或接进现有 FaaS 定时链），首评时点自加 cron 之日起算 | **已确认** | `.github/workflows/ingest-macro.yml:10-22`（`on: workflow_dispatch` + `push: paths: ops/ingest-macro.trigger`，`grep schedule\|cron` 零命中）；`ops/ingest-macro.trigger` 内容为 `1`，唯一提交 `f8650f8`（2026-08-05）；`docs/model_change_proposals.md:1041`（「数据层连续入库 ≥6 个月后方可首评」） | 是（要不要排期） | **08-07 本次新发现** |
| **DQ-02** 🟠 | 数据质量 | **P64-A：`macro_series` 存不下数据版本** ⟹ E47 判据①「只用发布日后可得值防前视」**在当前 schema 下不可能满足**。主键 `(period, series)` + upsert，统计局回溯修订会直接覆盖旧值、不留 vintage 轨迹；库里存的是「今天所知的历史」不是「当时所知的历史」。与 2026-07-25 复盘取数前视是同一类坑 | E47 首评前给表加 vintage 维度（`grep vintage\|publish_date\|release_date` 三处零命中） | **已确认** | `invest_model/data/schema.py:528-535`（`Column("period"..., primary_key=True)` + `Column("series"..., primary_key=True)`）；`scripts/ingest_macro.py:169`（`repo.upsert("macro_series", ..., ["period","series"])`）；`docs/model_change_proposals.md:2232-2255` | 否（P64 走执行纪律层验收，不需 E 判据） | 08-05 |
| **DQ-05** 🟠 | 数据质量 | **research 级投顾信号断流**：最后一份 `advisor_signals_20260712_weekend.csv`，其后全是 `intraday`+`grade=C`。直接后果是 OB-03（E18 零增长），间接后果是 P23/P24 两条提案的证据基础停止积累 | 恢复周末研报入库，或接受 E18 改时间兜底 | **已确认** | 同 OB-03；`docs/review_meta_audit_202608.md:138-148` | 是（属投顾输出节奏，非系统能改） | 08-02 |
| **MS-08** 🟠 | 量尺缺陷 | **恐慌分的贪婪端在 A 股两个最著名的顶部失灵**：2015-06-12（5178 疯牛）fear=**20.0**、2021-02-10（抱团顶）**28.9**，而 2025-08-25 一轮普通上涨是全样本最贪婪的 **0.0**；2021 全年 `fear≤10` 天数 **= 0**；2015-06-01~06-12 十天里九天精确 = 20.0（全样本恰等于 20.0 只有 24 天）＝限幅痕迹；`corr(Δfear, 当日收益) = −0.600`。⟹ **「顶部无对称锚」（P16/P19/E24/E37/E57 五连败）至少 E24「高价×极贪」那一支的失败有一部分是指标构造的失败，不是市场性质** | 凡依赖 fear **低分位（贪婪端）** 的分析必须加此标注；归因改为「顶部无锚 **或** 贪婪端失灵，本数据无法区分」 | **已确认**（D3 实证）。**恐慌端 ≥75 不受影响**（69 个 ≥75 日按月聚集全部命中真危机、无一落在平静期）⟹ E17/P22/P28/P30 全部维持 | `docs/specs/P70_data_findings.md:28-51`；`invest_model/signals/fear.py`（5 分量里 ①动量＝沪深300 距 MA125 乖离率本身、②波动率＝同一价格序列函数，见 `docs/specs/P70_spec_diff.md:86-94`） | 否（裁决一律不改，判据跑数前写死） | 08-06 |
| **OB-01** 🟠 | 后置义务 | **E51 卖出闸统一到 ×1.30 是 owner 明示越过判据的变更、不是 E51 通过**（判据① 红利 +0.27pp<0.50pp、判据② 创业板回撤恶化 7.1pp>3pp 均不过）；后置义务＝**≥2 个季度实盘记录后回看创业板那 7.1pp 回撤代价是否在真实路径上兑现** | 2026-08-05 起算 ⟹ **约 2026-11-05 后可首次回看** | **已确认**（已落地生产） | `docs/model_change_proposals.md:1747-1748`；同段记「代价：回撤 −31.1→−33.0 / −36.7→**−43.8** / −16.6→−20.1 / −31.2→−33.7，均仓 41→52%」 | 到期后需 owner 判断是否回滚 | 08-05 |
| **OB-02** 🟠 | 后置义务 | **E14 套利账本（P6）12 周影子期**：`ARB_ENABLED=1` 属账户级资产配置变更，**不适用高置信直升、判据全过后仍必须 owner 明示签核**；另有工程前置（fe-journey-faas `plan.ts` 与前端 Plan 页须先把 `defense_A/alpha` 行单独分区，不得混入「建议买入」三段——0713 事故的直接教训） | 影子期从 2026-07-14 起算，12 周 ⟹ **2026-10-06 后可首评** | **已确认**（影子数据持续积累中，不受注入 off 影响） | `docs/model_change_proposals.md:128-136` | 是（判据全过仍须签核） | 07-13 |
| **OB-07** 🟠 | 后置义务 | **P30 加杠杆信号**：owner 拍板直升（提示层零自动交易），后置义务＝**每次触发记录进复盘 + 累积 ≥2 次真实触发后复核前瞻表现**。⚠️ 与 MS-05 联动——**复盘里目前没有承接这条义务的段落**，触发了也不会被记下来 | ≥2 次真实触发（全期历史仅 2024-01/02 五天触发过一次） | **已确认**（义务已登记，承接段缺失） | `docs/model_change_proposals.md:627`；MS-05 的 grep 零命中 | 否（先修 MS-05 即可） | 08-01 |
| **OB-06** 🟡 | 后置义务 | **P20 投顾大盘风向环境闸**：owner 拍板直升、无历史可回测故验证义务后置＝提示行积累 **3-6 个月**记录后复核 | 2026-07-17 起算 ⟹ **2026-10-17 ~ 2027-01-17** | **已确认** | `docs/model_change_proposals.md`（P20 段）；CLAUDE.md「P20…验证义务后置＝提示行积累 3-6 个月记录后复核」 | 到期后需 owner 判断 | 07-17 |
| **OB-08** 🟡 | 后置义务 | **P51 容错自检行**：积累 3~6 个月后复核该行是否真的改变了 owner 的下单规模（参照 P20 后置口径） | 2026-08-04 起算 ⟹ **2026-11-04 ~ 2027-02-04** | **已确认**（提示行已上线） | `docs/model_change_proposals.md:964` | 到期后需 owner 主观判断（"有没有改变你的下单规模"只有 owner 能答） | 08-04 |
| **OB-04** 🟡 | 后置义务 | **E16 v2（国家队净申购 500 亿档）**：阈值系样本内事后选出，按治理不得直升；判据已写死（500 亿 / 超额≥+2pp / 胜率≥60%），**须未来新触发 episode ≥2 个样本外印证后才可进提示行**。当前五日滚动 426 亿未触发 | ≥2 个未来触发 episode | **已确认** | `docs/model_change_proposals.md:498` | 否（判据已写死，到条件自动首评） | 07-30 |
| **OB-15** 🟡 | 后置义务 | **E50 贪婪共振「大幅卖出」**：不上线、卖出规则维持无条件月卖 5%，但如实记为**待样本外印证的候选**（顶部方向五次尝试里表现最好的一次，夏普在两个组合口径/两个半段/两个 G 值下一致改善 +0.03~+0.07）；须再积累 **≥2 个未来触发年份**，届时按同一判据复评。**参数与判据一律冻结，不得为让它过关而调 G 或 F** | ≥2 个未来触发年份（2025 已触发 4 次可入观察） | **已确认** | `docs/model_change_proposals.md:1275-1284` | 否 | 08-04 |
| **OB-10** 🟡 | 后置义务 | **E52 v2 / E54 v2 须重测**（三方评审交叉校准后本轮判定无效）：E52 判据①指标失真（分母是各臂自己的最低买入价、跨臂不可比）+ 臂集不完整（缺 esc 族）；E54 因「棘轮是否复位」这一未经检验的实现选择单方面决定头条结论（改为跌回闸下即复位后创业板回撤 −56.8%→−27.5%、四腿夏普全优于现状）⟹ **E54 与 E54-b 本轮无效** | 重测设计要点已写死（`docs/model_change_proposals.md:2214-2222`），判据待跑三极点标定后另写 | **已确认**（生产代码零改动） | `docs/model_change_proposals.md:2206`（E52）、`:2208`（E54）、`:2010`、`:2037`、`:2214-2222` | 否（重测本身不需拍板；结论若要上生产才需要） | 08-05 |
| **OB-11** 🟡 | 后置义务 | 多个 E 脚本**已登记待实现**：E9（`scripts/validation/e9_index_ma60.py`）、**E9 v2**（`e9v2_regime_ma60.py`，条件化 MA60，方向已获全样本多周期正向初证）、E12（`e12_top_overlay.py`，P16 顶部 overlay）、P24/E19（action_plan 参谋异议行拼接处 + fear_daily 查询，提示-only 小改） | 无外部依赖，随时可实现 | **已确认** | `docs/model_change_proposals.md:277`、`:287-304`、`:359`、`:495`、`:529` | 否 | 07-16 ~ 07-27 |
| **OB-14** 🟡 | 后置义务 | **E23（P28 杠杆窗口）三信号联合回测待 Actions 跑**（需 fear_daily 回填数据）；价格双信号分支首跑 2 episode 全存活，但 **2008 与 2024-09 两个真实大底均不中价格双信号 ⟹ 恐慌③分支是必要腿**，其历史覆盖与联合回测尚未完成 | 恐慌深度回填已到 2015（`backfill_fear_deep.py` 上线），联合回测可跑 | **已确认** | `docs/model_change_proposals.md:577`、`:579` | 否 | 07-30 |
| **DQ-03** 🟡 | 数据质量 | **P64-B：`macro.py:seasonality()` 只实现了「同一自然月历史环比方向」那一支（判"假开门红"用的），库内无任何「1、2 月合并」**；而中国 CPI/PPI/社融 1、2 月不合并根本不可比，是陈老师每期第一个动作（合并后 CPI=0，再剔旅游仍在收缩＝两层校正）。topics 早记着这条但没实现 | 实现 1-2 月合并（走执行纪律层验收：算术可复核 + 不改仓位） | **已确认**（`grep jan_feb\|春节\|合并.*1.*2月 invest_model/signals/macro.py` 零命中） | `invest_model/signals/macro.py:71-83`（`seasonality` 只做逐月 `mom = d.diff()`）；`docs/model_change_proposals.md:2257` | 否 | 08-05 |
| **DQ-04** 🟡 | 数据质量 | **股息率口径的季节性未修**：新 D/P 口径用 `WIN=250` 交易日滚动，而 **250td ≠ 12 自然月**，且 A 股 85% 分红集中 5~7 月 ⟹ **每年重复计入一次分红季**（实测 6 月分位虚高 **+6.2pp**）。做季节内跨年比较前须先修 | 改为按自然月对齐的 12 个月窗口 | **已确认** | `scripts/analysis/index_dividend_yield.py:34`（`WIN = 250  # 滚动 12 个月（交易日）`）；CLAUDE.md「股息率口径已修…**未决**：250td≠12 自然月…6 月分位虚高 +6.2pp」 | 否 | 08-04 |
| **MS-09** 🟡 | 量尺缺陷 | **中证1000 作「从未参与调参的样本外对照」成色被高估**：**45.3%（2373 行）的历史早于其 2014-10-17 发布日**，且其 bias60 高尾**前 5 是 5/5、前 10 是 9/10 落在回溯段**；中证红利同理（高尾前 4 全在 2007-01，该指数约 2008-05 发布）。三条对照成色不同：**上证50 0%（✅ 名副其实）· 中证500 9.4%（⚠️ 基本干净）· 中证1000 45.3%（🔴 半段实盘中不存在）** | 今后引用 E24v2/E57/E58/E59 的样本外背书须逐条写明成色 | **已确认**（回溯段占比）＋ **待 owner 核实**（发布日由 D2/D3 依公开常识给出，本环境无法联网） | `docs/specs/P70_data_findings.md:53-70` | 是（发布日核实，见 OW-02） | 08-06 |
| **MS-11** 🟡 | 量尺缺陷 | **中证红利用的是价格指数 000922、不是全收益 H00922**（两文件都在仓库里），21.57 年累计差 **+116.1%**、年化 3.64%；`fwd_ret120` 全样本均值 +7.619% vs +9.411%（低估 1.792pp）。⟹ **绝对收益水平类主张不可用本数据包**（七腿全是价格指数、均不含股息）；**排名/分位/组差类可用**（两版 bias60 相关 0.997333、双尾前 3 前 5 完全重合）。副作用：分红集中 5-7 月使**红利腿 bias60 在 6-7 月被机械压低 1~2pp**、夏天多点亮低尾 | 凡出绝对收益水平结论须换 H00922；红利腿结论须做除息调整对照臂 | **已确认** | `docs/specs/P70_data_findings.md:93-104` | 否（不改 E56/E57/E59 裁决，归因加一句） | 08-06 |
| **MS-12** 🟡 | 量尺缺陷 | 研究数据包三处工程缺陷：①`%.10g` 落盘 ⟹ `ma120` 误差达 5e-6，**若哪条 E 判据写「残差 <1e-12」，从 CSV 复现会假性 FAIL**；②`dlnbias/leg_price/leg_ma/resid` **名字里没有 `fwd_` 但同样是前视的**（与 2026-07-25 复盘取数前视事故同型的温床）；③**七腿右端参差最多 3 个交易日**（创业板停 20260728，红利/上证50/中证1000 到 20260731）⟹「七腿当前乖离率横截面」实际在比不同日期的数 | ①②声称已修（`P70_data_findings.md` 记 F-5「均已修」）；③在**生产路径**（`action_plan.py:_index_bias_states` + 网站 `bias_rank` 横截面）是否同样参差**未核** | ①② **已确认已修**；③对生产路径的影响为 **疑似·待验证** | `docs/specs/P70_data_findings.md:106-113`；CLAUDE.md「P70 乖离率读数层落地」段（`index_bias_daily`、`/invest/bias`、前端「三之一」板块） | 否 | 08-06 |
| **DQ-06** 🟡 | 数据质量 | **fear 有硬截断与钳制段**：`fear==100` 六天（20150825/26、0901/02、0915、20160128）、`==0` 两天（20250825/26）；**`fear≥85` 共 31 天里 19% 顶格**，分桶 [95,100) 仅 2 天而 [100] 有 6 天＝典型截断堆积 ⟹ **`fear≥95` 那一档量的是截断值不是强度**。另 **2015-06-26（−7.87%）fear=72 压在 75 门槛下 3 分** ⟹ 触发日计数类判据须做 **±3 分敏感性** | 凡用 fear≥95 分档或触发日计数的判据须加此约束 | **已确认** | `docs/specs/P70_data_findings.md:115-121` | 否 | 08-06 |
| **DQ-07** 🟡 | 数据质量 | **fear 只覆盖 2015-01-05 起，且恰好切掉 bias60 高尾整段**：长历史五腿 46.3~48.7% 的行无 fear；**沪深300/中证红利/中证500 的 bias60 高尾前 20 天一天 fear 都没有（20/20）**。且分位阈值会漂移（中证红利 p95 从 +16.83%→+9.73%，−7.10pp）⟹ **全窗算分位阈值再 join fear 是口径错配**。**三者齐全的有效样本只剩 11.1 年、不重叠 120 日窗口 22.4 个（科创50 11.8）、独立恐慌事件 ≤8 个** ⟹ 日级显著性一律不采信，须 episode 聚合 + block bootstrap | 用 fear 就把窗写死 2015-01-05 起、分位在同窗内算 | **已确认**（`bias_meanrev_calib.py` 标定二/三/四/五仍用全样本分位，标记「须修」） | `docs/specs/P70_data_findings.md:76-91`、`:136-146` | 否 | 08-06 |
| **DQ-08** 🟡 | 数据质量 | 两个取数陷阱：①**`spread_full_history.csv` 的 `sse` 列是上证综指（1950~5166）、不是上证50（700~4732）**，误用会出大错；②**创业板单源无旁证**——`results/` 下没有 `index_dump_399006`，只能确认"忠实复制了 `chinext` 列"，**无法独立验证该列本身**（D1 与 S 位独立同报） | 补一份 `index_dump_399006` 作旁证 | **已确认** | `docs/specs/P70_data_findings.md:127-129` | 否 | 08-06 |
| **DQ-10** 🟡 | 数据质量 | 是否**仍在产生新的 buy +0 空指令**无法确认：生成侧已有降级防护（`shares_delta<=0` → `action="watch"`），表内 7 条 +0 集中在 0703–0707 判断为历史遗留；但本仓是 shallow clone（仅 50 个 commit、最早 2026-07-31），**无法用 git 历史确认防护上线时间** | 查库 | **待 DB 确认**：`SELECT plan_date, code, action, shares_delta FROM action_plan WHERE plan_date > 20260721 AND action='buy' AND shares_delta=0;` **应为空** | `invest_model/orchestration/action_plan.py:1193-1198`；`docs/review_meta_audit_202608.md:62-65` | 否 | 08-02 |
| **RT-03** 🟡 | 运行时健康 | **live_watch 定时器心跳丢失**（20260805 14:00 检出 >20 分钟未更新）＝疑似 timer 静默丢失；恐慌哨兵已代跑并每 5 分钟持续接管。与 daily 17:00 同型（0714-0716 三连静默丢失） | 排查 FC `invest-live-watch` 定时器 | **已确认**（单次事件；是否复发需持续观察） | issue #11 [comment 5188152031](https://github.com/AmbitionC/invest-model/issues/11#issuecomment-5188152031)；`faas/jobs.py:297-330`（盯盘互援哨兵实现） | 是（与 RT-01 同一排查） | 08-05 |
| **RT-04** 🟡 | 运行时健康 | **哨兵已从兜底变主力路径，且其自身无二级兜底**（RT-01 的结构性根因）。同类历史：0716/0717 连续两日 `ingest_etf` 静默丢失（哨兵 v2 已加 ETF 查缺补跑） | 与 RT-01 合并处理 | **已确认** | `faas/jobs.py:438-446`（docstring 记 0714-0716 三连丢失、0715 整组重建无效）、`:469-489`（ETF v2 兜底）；`docs/review_meta_audit_202608.md:189-191` | 是 | 08-02 |
| **MS-07** 🟡 | 量尺缺陷 | 复盘第三段**快照日与报告 asof 不同步且未标注**：标题「截至 20260731」，第三段持仓用 **20260728** 快照（差 3 个交易日）；「区间归因」用 0727→0728 **相邻两天**当作「区间」。两处都未标差值，易被读成当日口径 | 标注快照日与 asof 差值 | **已确认**（08-01 报表原文） | issue #8 [08-01 复盘](https://github.com/AmbitionC/invest-model/issues/8#issuecomment-5151100582)第三段；`docs/review_meta_audit_202608.md:203-206` | 是（元审计建议⑤同批） | 08-02 |
| **MS-10** 🟡 | 量尺缺陷 | **模型段告警疲劳**（元审计列为「呈现问题不是数据问题」，附带观察不作问题）：模型月度调仓，周报里该段每月有 3 周是重复旧数，⚠️ 警示行会连挂 4 周。已排除「模型停更」怀疑（`closed_loop.py:55` 是 `rebalance="monthly"`，07/01 是最后调仓点、下一点 08-03） | 加月度口径标注（A3 修订已加，是否够待观察） | **已确认**（属正常节奏，无异常） | `docs/review_meta_audit_202608.md:210-218`；`invest_model/orchestration/closed_loop.py:55`（`rebalance: str = "monthly"`） | 否 | 08-02 |
| **OW-01** 🔴 | 待拍板 | **元审计 8 条建议至今零采纳**（08-02 提出，08-07 逐条 grep 复核：`no_op` 零命中 · `n_h10` 零命中 · `broad\|leverage\|宽基` 零命中 · `集中度\|concentration` 零命中 · `latest.json` 仍 20260724）。原文明写「均未执行，待 owner 拍板」「本次未改动任何生产代码、未登记任何 P/E 提案」 | owner 逐条批 | **已确认** | `docs/review_meta_audit_202608.md:220-240`（建议表 8 行）；本次 grep 复核见 MS-01/03/05/06 + RT-02 | **是（这是本台账的总闸）** | 08-02 |
| **OW-02** 🟠 | 待拍板 | **ETF 上市日 / 指数发布日须 owner 联网核实**——这是宽基策略三个否决项（红利腿 19.5 年里约 66% 标的不存在、科创50 起点 20191231 是回溯基日、"+4.37pp 撤回"）的**唯一事实依据**，V2 凭公开常识给出、本环境无法联网。已做三档敏感性、结论对档位不敏感。同批待核：中证1000 2014-10-17、中证红利约 2008-05 发布日（MS-09） | owner 联网核对 | **已确认**（待核状态本身） | `docs/broad_index_playbook.md:231`（「ETF 上市日是 V2 凭公开常识给出的，本环境无法联网核实，须 owner 确认」）；`docs/specs/P70_data_findings.md:70` | **是** | 08-04 / 08-06 |
| **OW-03** 🟠 | 待拍板 | **标的清单出入（直接呈报、不立 E）**：R1 查证陈老师 5 份宽基清单中**中证500 出现 5/5 次，而科创50 与中证红利不在他任何一份清单里** ⟹ 系统四条腿中有两条是他从未主张过的标的。中证500 此前作为「从未参与调参的对照指数」用统一 (1.00,1.30) 测过、结果为正。**账户级配置变更不适用高置信直升，判据全过仍须 owner 签核** | owner 决定四腿标的是否调整 | **已确认** | `docs/model_change_proposals.md:1893-1898` | **是** | 08-05 |
| **OW-04** 🟡 | 待拍板 | **P33 科创50 腿由定投改 L50 阶梯**：状态「**建议采纳**，待 owner 签核」；红利腿维持"滚动5年锚"（E28 结论），上证50 不在篮内暂不处理 | owner 签核 | **已确认** | `docs/model_change_proposals.md:661` | **是** | 08-01 |
| **OW-05** 🟡 | 待拍板 | **P34 腿权重与资金分配**：W0~W3 已定义，**W4「owner 主观权重」待 owner 给定**，缺此档无法完成该组对照 | owner 给权重 | **已确认** | `docs/model_change_proposals.md:668` | **是** | 08-01 |
| **OW-06** 🟡 | 待拍板 | **P32/E26 红利腿估值锚待 owner 补充材料**（登记待材料线）；E28 同段记「E26 估值锚验证仍待 owner 材料」 | owner 提供 PB/股息锚材料 | **已确认** | `docs/model_change_proposals.md:896`、`:646` | **是** | 08-01 |
| **OW-07** 🟡 | 待拍板 | **E34 判据修订建议待 owner 批**（M1-M3 三条方法论产物）：方向准确率按 episode 起始日计；月频信号 \|ρ\| 门槛提到 ≥0.25 或改 block-bootstrap；**终裁须披露同批候选总数做 FWER 校正**。背景：判据②（独立 episode ≥30）是唯一真正把关的条款、**判据①基本是摆设**（纯噪声误过率 12.4%~27.6%，K=15 候选时「至少一条纯噪声过①②」概率 94.9%） | owner 批 | **已确认** | `docs/model_change_proposals.md:696` | **是** | 08-02 |
| **OW-08** 🟡 | 待拍板 | **E9 v2 晋升路径待 owner 拍板**：证据形态符合高置信直升（判读标准预登记、全市场 7.5 年样本、8 变体预列明无参数寻优、结论指向最简变体），**但入场闸放宽属策略行为变更须 owner 明示同意**。选项 A 直升生产 / B 影子（计划里并行标注"v2 触发"不生效，观察 4 周再切） | owner 选 A 或 B | **已确认** | `docs/model_change_proposals.md:433` | **是** | 07-19 |
| **OW-09** 🟡 | 待拍板 | **P53 执行纪律层待实现项（登记，暂不实现）**：①「调结构优先于调仓位」需先定义系统内的"结构"维度（风格？行业？腿间权重？），**定义不清前不实现**；②「只减不清 / 永不做空」目前无违反路径（系统零自动交易），记为硬约束文档条目而非代码 | owner 定义"结构"维度 | **已确认** | `docs/model_change_proposals.md:977-984` | **是**（①需 owner 定义） | 08-04 |
| **RT-05** 🟡 | 运行时健康 | **校准位 S（SOP §7.8）两个实现缺陷，上午写下午首次执行即暴露**：①**信息隔离被工具链单方面破坏**——三仓 CLAUDE.md 被**自动注入** S 位上下文（它主动披露），prompt 约束挡不住 ⟹ **今后「S 与主线一致」不再算证据，只有「不一致」才有效力**；②**时序证明失败**——主线规格那次 commit 因 cwd 不在仓库内报错、且 S 的产物比主线先落盘 ⟹ **主线规格必须在 spawn S 位之前就 commit+push**；③补：**S 位不能替代主线对实现的核查**（被禁读代码，结构上不可能发现「恐慌分含价格派生分量」这类事实） | 三条修订已写入 §7.8，下次执行 S 位时验证是否生效 | **已确认** | `docs/specs/P70_spec_diff.md:11-43`、`:86-97`；`docs/model_change_proposals.md:2085` | 否（修订已写死） | 08-06 |

---

## 二、分类小结

| 类别 | 条目数 | 最紧急的一条 |
|---|---|---|
| **量尺缺陷** | **12**（MS-01~12） | **MS-01** `buy_fill_rate` 空指令污染——买入执行率虚高 89% vs 真实 50%，已连续两期误报，且明天（08-08）复盘会第三次误报。它污染的是「买入守纪律 / 卖出不守纪律」这个直接影响执行行为的结论 |
| **后置义务** | **10**（OB-01~15，非连号） | **OB-05** E47 的「连续入库 ≥6 个月」时钟根本没在走（ingest-macro 无 cron）——**这是本次新发现**；等到 2027-02 首评时才发现序列不存在，就白等半年 |
| **数据质量** | **9**（DQ-01~10，非连号） | **DQ-01** 持仓快照断更 6/10、6 条指令完全无法对账——每断更一天，执行对账能力就永久少一天，**且 #263 之后补录路径变长**，越拖越难补 |
| **运行时健康** | **5**（RT-01~05） | **RT-01** 17:00 定时器连续 15 个交易日失效，且 08-03 起 17:40 哨兵补跑疑似也失败（18:30 二次告警）——**这是唯一可能导致当日计划整日不出、风控指令不下达的问题** |
| **待拍板** | **9**（OW-01~09） | **OW-01** 元审计 8 条建议零采纳——它是 MS-01/03/05/06 + RT-02 五条的总闸，不批这一条，量尺就一直是歪的 |
| **合计** | **45** | |

**证据分级分布**：`已确认` **38** 条 · `已确认 + 疑似部分` 4 条（RT-01 / MS-02 / MS-04 / MS-12）· `待 DB 确认` 1 条（DQ-10）· `待 owner 联网核实` 2 条（OW-02、MS-09 的发布日）。

---

## 三、时间敏感项（本周内会发生什么）

**今天 2026-08-07 是周五。**

1. **今天 17:40 与 18:30**：issue #11 大概率再收两条 `plan_watchdog` 告警（08-03 起连续 4 个交易日都是双发）。**如果只收到一条，说明 17:40 的补跑成功了，RT-01 的「哨兵自身失效」判断需要下调**；如果又是两条，就是第 5 个交易日双发，应立即排查 FC 定时器。
2. **明天 2026-08-08（周六）FaaS 自动跑 `job_weekly_rebuild_review`**（全量重建 → P4 影子回测 → 套利记分卡 → 复盘推送 issue #8）。届时**确定会发生**：
   - 新复盘**仍会**用被污染的 `buy_fill_rate`（MS-01 第三次误报）；
   - 分级表**仍无** `n_h10`（MS-03），两列排序矛盾仍无法裁定；
   - **仍无**集中度行（MS-06）、**仍无**宽基/杠杆段（MS-05）；
   - `results/review/latest.json` **仍停在 20260724**（RT-02），与新复盘的差距从 15 天拉大到 **约 22 天**——任何按 `review_schema.md` 从零上下文读 JSON 的 Agent 会拿三周前的数据当最新；
   - 若快照仍断更，本周 0803–0807 的指令下周会变成新一批「无法对账」，覆盖率从 6/10 继续下滑（DQ-01）。
3. **E18 影子样本本周不会增长**：research 级信号断流已约 4 周（最后一份 20260712_weekend），样本冻结在 73/100（OB-03、DQ-05）。
4. **本周无任何 E 到达首评窗口**：最近的两个是 E14（2026-10-06）与 OB-01 的 E51 回看（约 2026-11-05）。

> **建议在 08-08 复盘跑之前处理的**：只有 MS-01 与 RT-02 两条能在一天内改完且立刻改变明天的产出（见下节顺序 1、2）。其余的等复盘跑完再动不损失什么。

---

## 四、建议的处理顺序与改动面

| # | 条目 | 为什么排这里 | 改动面 |
|---|---|---|---|
| 1 | **RT-01 + RT-03 + RT-04** | 唯一会导致**计划整日不出**的问题，且今天下午就会再复现一次。先看日志确认 17:40 补跑到底失败在哪 | **生产链路**（FC 定时器排查；哨兵二级兜底属新增代码，需 owner 批） |
| 2 | **MS-01**（`no_op` 状态） | 明天复盘就会第三次误报；改动极小、纯只读层、不碰任何投资决策逻辑 | **只读层小改**（`execution.py` 加一个 status 分支） |
| 3 | **RT-02**（JSON 双轨） | 同样赶在 08-08 之前；**两条路任选其一**——FaaS 侧经 GitHub API 提交 `latest.json`，或直接改 `review_schema.md` 的口径声明（后者 10 分钟、零风险） | **纯文档登记**（改声明）或 **生产链路**（FaaS 提交） |
| 4 | **DQ-01**（快照断更） | 每拖一天就永久少一天对账能力；但补录路径需 owner 定（FaaS API vs 本地脚本直连 DB） | **生产链路** + 需 owner 定路径 |
| 5 | **MS-03 + MS-06 + MS-07**（`n_h10` / 集中度行 / 快照日差值标注） | 三条都是复盘呈现层，可一次改完；MS-03 是 P23 能否裁定的前提 | **只读层小改**（`review.py` facts 构造 + 呈现） |
| 6 | **MS-05 + OB-07**（复盘第七段承接宽基/杠杆） | P30 的后置义务需要它才成立；但改动比 5 大 | **只读层新增一段**（`review.py` 新段 + 读 `broad_leg_state`/`leverage_signal`） |
| 7 | **MS-02**（告警熄火降级为历史行） | 机制本身是对的，改的是呈现方式；晚于 5/6 无损失 | **只读层小改**（`review.py` 呈现层） |
| 8 | **OB-05**（给 ingest-macro 加 cron） | 时钟越早启动越早能首评，但 E47 本来也要等 6 个月，不急在一周 | **生产链路小改**（workflow 加 `schedule`） |
| 9 | **DQ-02 + DQ-03**（macro vintage 维度 + 1-2 月合并） | E47 首评前必须修，但首评在半年后；且 DQ-02 是**建表变更**，越早改越便宜（数据越少迁移越轻） | **生产链路**（`schema.py` 加列 + `ingest_macro.py` 改主键） |
| 10 | **MS-04 + OB-03**（E18 v2 并列口径 + 时间兜底） | 只改判据登记方式、不改判据数值、不触发任何执行动作；但 P23 的实际执行仍须 owner 签核 | **纯文档登记** |
| 11 | **OW-02 ~ OW-09**（七条待拍板） | 全部阻塞在 owner，系统侧无事可做；建议一次性打包呈报 | **纯文档登记**（呈报） |
| 12 | **OB-10 + OB-11 + OB-14**（E52v2/E54v2 重测 · E9/E9v2/E12/E19 脚本待实现 · E23 联合回测） | 研究线，不影响生产；判据/设计要点均已写死，随时可跑 | **只读层**（`scripts/validation/`、`scripts/analysis/`，不落库） |
| 13 | **MS-08 ~ MS-12 · DQ-04 ~ DQ-10 · RT-05** | 已记录在案的口径约束与陷阱，作用是**约束未来的分析**而非修复现状；**裁决一律不改**（判据跑数前写死） | **纯文档登记**（引用时加标注） |

---

## 五、复现与复核

```bash
# 元审计两条实证结论（退出码 0 = 复现）——2026-08-07 复跑通过
python scripts/analysis/review_meta_audit.py

# 元审计 8 条建议的采纳状态（本次全部零命中 = 全未采纳）
grep -n "no_op"                     invest_model/review/execution.py
grep -n "n_h10"                     scripts/review.py
grep -n "broad\|leverage\|宽基"      scripts/review.py
grep -n "集中度\|concentration"      scripts/review.py
python3 -c "import json;print(json.load(open('results/review/latest.json'))['report_date'])"   # → 20260724

# P64 两条数据层缺陷
grep -n "vintage\|publish_date\|release_date" invest_model/data/schema.py scripts/ingest_macro.py   # → 零命中
grep -n "jan_feb\|春节\|合并.*1.*2月"          invest_model/signals/macro.py                        # → 零命中

# OB-05（本次新发现）
grep -n "schedule\|cron" .github/workflows/ingest-macro.yml   # → 零命中
```

**待 DB 确认项汇总**（本环境无凭据）：

```sql
-- DQ-10：生成侧防护是否仍在漏空指令（应为空）
SELECT plan_date, code, action, shares_delta FROM action_plan
 WHERE plan_date > 20260721 AND action = 'buy' AND shares_delta = 0;

-- RT-01：17:40 哨兵补跑是否真的失败（08-03~08-06 应各有 >0 行；若为 0 则补跑确实没写进去）
SELECT plan_date, COUNT(*) n FROM action_plan
 WHERE plan_date IN ('20260803','20260804','20260805','20260806','20260807')
 GROUP BY plan_date ORDER BY plan_date;

-- DQ-01：快照断更的当前范围
SELECT snapshot_date, COUNT(*) n FROM holding_snapshot
 WHERE snapshot_date >= '20260720' GROUP BY snapshot_date ORDER BY snapshot_date;

-- OB-03：影子样本是否仍冻结在 73
SELECT COUNT(*) n, SUM(gate_date IS NOT NULL) hit FROM policy_shadow;
```

---

# 四、交叉验证增补（2026-08-07·三条独立通道）

台账主表（上文）由素材整理通道产出。同批另跑三条**独立交叉验证通道**——数据层 / 策略规则层 /
指标层，各自只读、互不通气。原始报告全文入库：

- `docs/xval_20260807/xval_data.md`（数据层，391 行）
- `docs/xval_20260807/xval_strategy.md`（策略规则层，326 行）
- `docs/xval_20260807/xval_indicator.md`（指标层，553 行）

## 4.1 证据分级（沿用元审计口径，**不可跳过**）

三条通道都被写死一条纪律：**工具链会自动注入三仓 CLAUDE.md，因此「与主线文档一致」不构成
证据，只有「不一致」才有效力**（SOP §7.8 2026-08-06 首次执行时发现信息隔离被工具链单方面
破坏，该教训的直接应用）。下表据此分两级：

| 标记 | 含义 | 可否作为改动依据 |
|---|---|---|
| **【主线已复核】** | 主线**独立跑过验证**，附复现命令与实测读数 | ✅ 可 |
| **【通道报告·未复核】** | 通道的结论，主线**尚未复算** | ❌ 先复核再动手 |

> 🔴 **通道报告未经复核不得直接改生产。** 本轮已出现一次「通道结论严重度偏高」的实例，
> 见 4.4 —— 这正是元审计立分级制度的原因。

## 4.2 主线已复核的发现（可作为改动依据）

| ID | 发现 | 复现方式与实测读数 | 影响面 |
|---|---|---|---|
| **XV-01** 🔴 | **「唯一真源」只对卖出闸成立**。`BUY_MUL` 在生产代码里**零引用**——只出现在 `broad_now_chart.py` / `broad_history_chart.py` / `broad_export_web.py` 三个画图与导出脚本中；生产 `action_plan.py` 的买入闸是字面量。而 `broad_export_web.py:104` 把 `buy_mul` 导出到网页，网页标题写着「生产与回测同一份定义」 | `grep -rn "BUY_MUL" --include=*.py .` → 命中 8 处全在 analysis 脚本 | 🔴 P58「物理上不可能再漂移」的保证**只覆盖一半**；网页对外声明与事实不符 |
| **XV-02** 🔴 | **科创50 阶梯买点（B3）生产里根本没实现**：谓词写死 `lambda c, m, r: False`，而同一行的描述文案是「深回撤阶梯 L50（距全历史峰 −50/−55/−60/−65 四档，各档一轮只买一次…）+ 恐慌抢买」 | `sed -n '330,333p' invest_model/orchestration/action_plan.py` | 🔴 该腿生产买入**只能靠 `fear≥75` 触发**，与文档描述的四档阶梯完全不同 |
| **XV-03** 🔴 | **恐慌分支覆盖卖出闸**：状态机 `if price_buy → elif panic → elif fsell`，`panic` 排在 `fsell` **之前**，故 `fear≥75` 时即使价格已过卖出闸也显示「🟢恐慌抢买窗」。而同函数 docstring 写的是「panic＝**价格未到**但恐慌≥75」⟹ **意图与实现错配**。`panic` 判定只看 `fear>=75`，**无价格闸、无冷却** | `sed -n '371,379p' invest_model/orchestration/action_plan.py` | 🔴 与 P22/E17 的约束（「必须独立成腿、不走 detect_buypoints」「不改任何闸门」）有直接张力。系统零自动交易故不会自动下单，但对靠提示驱动手动操作的系统＝**误导性提示** |
| **XV-04** ⚠️ | **乖离率排名 off-by-one**：当日 bias 用 `c[-60:].mean()`（切片），窗口内同一天用 `rolling(60).mean()`，浮点差 4.44e-16 ⟹ **今天自己不等于自己**，`rank_low`/`rank_high` 之一虚增 1。可机检不变量 `rank_low + rank_high == win + 1` 被破坏 | 主线实测**最新一日六腿**：中证红利 `1650+852=2502 ≠ 2501` ❌（其余五腿 =2501 ✅）。复现见 4.5 | ⚠️ 仅提示层。虚增只会让「进前 4」🚨 **漏报**（偏保守），不误报，不碰买卖闸 |
| **XV-05** 🔴 | **宽基四腿 3/4、P70 七腿 5/7 没有任何日更路径**：`update.py:16` `BENCHMARKS = ["000300.SH","000905.SH","000906.SH"]`，而四腿需 399006 / 000688 / 000922，P70 再加 000016 / 000852。`backfill_index.py` 虽列了这些代码，但那是**手动 trigger 的回填脚本**，非定时日更 | `grep -rn "BENCHMARKS" invest_model/` | 🔴 **越过提示层**：陈旧价格会进 P26/P27/P31 的窗口判定 |
| **XV-06** 🔴 | **宏观数据层的时钟根本没在走**：`ingest-macro.yml` 的 `on:` 只有 `workflow_dispatch` + trigger 文件 push，**无 cron**；`ops/ingest-macro.trigger` 自建仓仅 commit 过 1 次；FC `JOBS` 注册表**无 `ingest_macro`** | `grep -nA6 "^on:" .github/workflows/ingest-macro.yml`；`grep -n "JOBS = {" -A14 faas/jobs.py` | 🔴 E47 的准入前提「数据层**连续入库 ≥6 个月**」当前**根本没在积累**；按现状要到 2027-02 首评时才会发现序列不存在 |
| **XV-07** ⚠️ | **51 个 workflow 零 pytest**——没有任何 CI 跑测试 | `grep -rl "pytest" .github/workflows/ \| wc -l` → `0` | ⚠️ 上述 XV-01/02/03 三类「实现与文档不符」缺乏自动拦截网 |

## 4.3 通道报告的发现（**未复核，先复算再动手**）

按通道归档，主线**均未复算**。数字引自各通道报告，复现方式见各自原文。

**数据层**（`xval_data.md`）
- XV-04 的**全历史尺度**：29,794 天中 13,352 天（44.8%）错位、方向单边、前 4 🚨 漏报 22 天（占真极值日 411 天的 5.4%）；「CLAUDE.md 载创业板第 13 低应为 12」。**主线只复核了最新一日，此统计未复算。**
- `_persist_index_bias_daily` 一律把 `trade_date` 盖成计划日、丢弃真实收盘日 ⟹ 陈旧收盘会被当今天上邮件上网站，零告警（与 XV-05 叠加放大）。
- 宏观层三处：`melt_frame` 的 `drop_duplicates(keep="last")` 会把 `yc_cb` 国债**收益率曲线压成一个点**；`_ALIAS["sf_stock"]` 缺 `sf_month` 回退致第④项可能恒 `None`；`seasonality()` 拿到的是 M1 **同比**而非 docstring 明写的**余额环比**，digest 文案照错的印。
- 正面：数据包 7/7 md5 逐字节可重建、恒等式残差 ≤3.4e−15、零日历缺口、价格底座常识对撞全过 ⟹ **问题在读数实现与刷新链路，不在数据本身**。
- 自述覆盖缺口：`macro_series` **一行真实数据都没见过**（宏观三处全依赖待跑 SQL）；本轮 96 个改动文件里约 55 个 `scripts/analysis/*` 未审。

**策略规则层**（`xval_strategy.md`）
- 扰动实验：`SELL_MUL`→9.99 四腿状态全变 ✅ / `BUY_MUL`→1.60 状态一个没变 ❌（XV-01 的来源）。
- B2 恐慌腿丢价格闸与 20 日冷却的**历史尺度**：48/40/3/55 天生产报「🟢抢买窗」而规格不该买，其中 20/34/3/54 天该腿正在卖出区；最近一次 2026-07-13/17/20，创业板在卖出闸上方 39% 时连报三天。
- 网页上「买入线」与「乖离率排名」**各有两个互斥定义**同屏（科创50 med×1.00 vs peak×0.50；创业板「历史第 92 低」vs「近十年第 12 低」），**被文档判错的全历史口径仍在生产落库**。
- 可跑性：`long_window_backtest.py` 裸跑仍 `ModuleNotFoundError`（缺 sys.path 引导），加 `PYTHONPATH` 后跑通且数与文档逐位一致；`broad_playbook.py` EXIT=1（`:236` 写死 `Path(".")`）；**`broad_trades_chart.py` 与 `e37_deviation_top.py` 仍指着 scratchpad 时代的 `hs300.csv`，均跑不起来——E37 是网站上引用的裁决，其复现脚本是死的**。
- `broad_trades_chart.py` 是 08-05「建立唯一真源」**之后**才加的，却又抄了硬编码闸位、scratchpad 文件名、以及红队 F1 已判虚增 +4.7pp 的 `20200601` 硬起点。
- `long_window_backtest.py:441` 图标题仍印着已作废的「策略把 70% 回撤压到 17~44%」；取代它的「诚实读数」`:169-174` 用布尔掩码把**不连续**的日子拼起来求 running max ⟹ 不是任何真实时间段的回撤。
- 科创50 恐慌腿 6.6 年里有 **5.1 年结构性关闭**（`r1250` 在各腿自己 CSV 上滚，首个可用日 2025-03-03），caveats 只提「2015 前无恐慌数据」。
- `pytest` 199 passed / **3 failed**（2 个 crowding 断言属既有）；新增 `test_macro_layer.py` 缺 tushare 时**中断整个 collection**。
- `_base_sleeve_hint`（P27 底仓 25%）是**死代码**但文档仍称在跑；P52 与状态机对「科创50 是否触发买入」判定不一致（829 个历史日）；`_fear_score` 会沿用陈旧恐慌值。
- 报告第 4 节列了 **21 条「未写进判据的实现选择」**（含位置、两个分支、有无对照臂）——**其中只有 WARM=500 一条做过对照臂**。

**指标层**（`xval_indicator.md`）
- **贪婪端不是「失灵」，是符号相反**：四个方向性分量在强势上涨里同时触地，此时 `score ≡ f_vol/5`，域被压进 [0,20] 且随波动**单调递增** ⟹ 熔涨顶读数**下限即 20.0，越欣快越显得「恐慌」**。报称 2015-06-12 与 2024-10-08 分量画像逐字相同。**若成立，比 CLAUDE.md 记的 F-1「贪婪端失灵」更精确。**
- `Δscore` 方差 **92.9%** 来自三个横截面分量（「5 等权」是名义的）；**46.2% 交易日＝极度贪婪** ⟹「高价 AND 极贪」只筛掉 47%（E24 的增量解释）。
- 恐慌分是**小盘宽度温度计**：`|corr(Δscore, 当日收益)|` 按市值严格单调，中证1000 0.729 → 上证50 0.468 ⟹ 读数对小盘正确却给大盘腿开抢买窗＝**宇宙错配**。
- 20 个 `fear≥75` episode 里 6 个发生在沪深300 已过 ×1.30 时；2026-07-13/17/20 那轮 250 日回撤仅 −10.5%＝全部 episode 里最浅 ⟹ **与 CLAUDE.md「69 个触发日全部命中真危机」不一致**（🔴 若复算成立，波及 F-1 对 E17/P22/P28/P30「全部维持」的裁决）。
- `BIAS_TOPK=4` 未按窗长归一，科创50 触发率 1.63×；`margin_ratio` 34% 日度方差来自含价格的分母；股息率 250td **单边只多计**（max +1.618pp），8 月~次年 4 月分位被压低 0.8~1.8pp（CLAUDE.md 只提 6 月）。
- 宽度分量把停牌/退市 NaN 计为「跌破 MA20」、与同函数内 `valid` 掩码不一致〔待DB〕；涨跌停固定 9.8% 阈值漏 ST、错分 20% 制度股〔待DB〕。
- **如实记录的三条证否**：未复权价夏季季节性仅 0.9 分、`clip(lower=0)` 整流偏差仅 +0.02%、基底 CSV 全部升序——原假设均不成立。
- **P26 中位线生产 6 处 + 回测定义完全一致，未发现 P58 类型漂移；未发现任何真正的前视。**

## 4.4 主线对通道结论的一处下调（留痕）

台账 RT-01 原判「这是唯一可能让当日计划**整日不出**的问题」。主线复核：issue #9 在
**2026-08-06 19:43** 有更新，说明 18:30 那道补跑成功、当日计划最终产出，只是比 17:00 晚约
2.7 小时。**准确表述是「三道防线倒了两道、只剩最后一道」，不是「计划不出」。**
（主线只逐日核实了 08-06；08-03~08-05 是否同样兜住未单独查。）

严重度虽下调，**优先级不降**：哨兵已从兜底变成唯一路径，且其自身无下一层。

## 4.5 复现

```bash
python scripts/analysis/review_meta_audit.py        # 元审计两项，退出码 0 = 复现
grep -rn "BUY_MUL" --include=*.py .                 # XV-01：命中全在 analysis 脚本
sed -n '330,333p'  invest_model/orchestration/action_plan.py   # XV-02：谓词恒 False
sed -n '371,379p'  invest_model/orchestration/action_plan.py   # XV-03：panic 排在 fsell 前
grep -rn "BENCHMARKS" invest_model/                 # XV-05：日更只覆盖三个指数
grep -nA6 "^on:" .github/workflows/ingest-macro.yml # XV-06：无 cron
grep -rl "pytest" .github/workflows/ | wc -l        # XV-07：0
```

XV-04 的不变量检查（`rank_low + rank_high` 应恒等于 `win + 1`）：对每条腿按生产口径
（当日用 `c[-BIAS_MA:].mean()`、窗口用 `rolling(BIAS_MA).mean()`）算完取和比对即可，
主线实测最新一日中证红利为 2502、其余五腿 2501。**建议把该不变量做成断言常驻**——
它能在无需 DB 的情况下捕获这类 off-by-one。

## 4.6 主线的结论

1. **XV-01/02/03 是同一类病**：生产实现与文档/网页对外声明不符，且三处都**不在任何判据的
   检查范围内**。E 判据检查的是「策略有没有效」，没有一条检查「代码有没有按规格实现」。
2. **XV-04 的意义大于它的影响**：它只造成提示层漏报，但它**五个交叉验证通道全都抓不到**
   ——因为那些通道重写的是**研究引擎**，而这个 bug 在 `orchestration/` **消费端**。这与
   `docs/review_meta_audit_202608.md` 的核心论点同构：**校准体系覆盖了策略研究，没覆盖每周
   真正驱动决策的那一层。**
3. **XV-06 与 E47 的关系值得单列**：判据写得再严，若数据管道没有定时器，判据到期时无数可评。
   **今后凡「积累 N 个月后首评」类判据，登记时必须同时确认采集侧有定时触发路径。**
4. 元审计 8 项至今**零采纳**（第三次复核确认），本次新增 7 条已复核项。**建议先修量尺、
   再谈策略**——XV-01/02/03 决定了当前所有宽基相关读数的可信度。

---

# 五、2026-08-07 夜间批次（owner 授权全权执行）

本节记录 0807 当晚一次性完成的修复与新查实的问题。owner 指示「决策依据按你的来」，
故以下处置均由主线判断，逐条留痕理由。

## 5.1 本批已修复并推送

| # | 问题 | 根因（实证） | 处置 | commit |
|---|---|---|---|---|
| F-1 🔴 | **宽基四腿三腿价格陈旧 5~8 个交易日** | `update.BENCHMARKS` 只含 000300/000905/000906，另三腿不在日更清单 ⟹ `_index_hist_by` 的 `index_daily` 增量取不到新数据、只能停在基底 CSV 末行。**逐位吻合非推断**：创业板 3327＝CSV 末行 `20260728,3327.0298`、科创50 1588＝`20260730,1588.4149`、红利 5569＝`20260731,5569.4095`，而计划口径行标着「行情/决策日 20260807」 | BENCHMARKS 补 399006/000688/000922/000016/000852；四腿逐腿新增 `data_date`，陈旧即在计划正文出 🔴 告警行并写明自检语 | `618dc6f` |
| F-2 🔴 | **research 信号永不过期** | `ingest_advisor.py` 补默认失效日只有 intraday 分支，research 留空即永久有效。退出通道 `direction=exit` 需投顾明确宣告逻辑证伪，实践中从不发生 ⟹ 104 条信号无失效日、最老 20260611（近两个月），观察池 8 只当日模型全部看空/偏空 | owner 拍板 30 天复核期：新增 `RESEARCH_VALID_DAYS=30`；回补 104 行，83 条即时失效 | `f89bfed` |
| F-3 ⚠️ | **盘中挂单像可执行指令** | `live_check.py` 从不调用 `detect_buypoints`。收盘「建议买入」＝技术×量化(rank≥0.5)×环境(gross) 三层；盘中只判 MA60+价位。**0728 锐捷教训（次日 −9%）当时只修了突破分支**，回踩分支保留给量、理由写「与主闸语义一致」——该理由不成立（主闸回踩要放量阳线+rank+环境，盘中只要 \|dev\|≤3%） | 回踩挂单统一附「⚠️仅价位到位，是否可买以收盘三层闸为准」；更正那条已作废的注释 | `d011e6a` |

## 5.2 本批查实但**未修**（需 owner 决策或工作量较大）

| # | 问题 | 严重度 | 说明 |
|---|---|---|---|
| G-1 | **科创50 阶梯买点生产里没实现** | 🔴 | `action_plan.py` 谓词写死 `lambda c, m, r: False` 恒假，而同行文案是「深回撤阶梯 L50 四档」、图表也画着「阶梯第一档 1104」。该腿生产买入实际只能靠 `fear≥75` 触发。**价格真跌到 1104 不会触发任何东西** |
| G-2 | **恐慌分支覆盖卖出闸** | 🔴 | 状态机 `if price_buy → elif panic → elif fsell`，`fear≥75` 时价格已过卖出闸仍显示 🟢恐慌抢买窗；docstring 写的是「panic＝**价格未到**但恐慌≥75」＝意图与实现错配。与 P22/E17「必须独立成腿、不改任何闸门」的约束有张力 |
| G-3 | **`BUY_MUL` 生产零引用** | 🔴 | 「唯一真源」只对卖出闸成立；买入闸在生产与全部回测里是字面量。而 `broad_export_web.py` 把 `buy_mul` 导出到网页、页面标题写着「生产与回测同一份定义」 |
| G-4 | **元审计 8 项仍零采纳** | ⚠️ | 第四次复核确认。`buy_fill_rate` 空指令、`n_h10` 缺失、JSON 双轨断链、告警熄火留痕、集中度行、第七段——全部未动 |

## 5.3 复盘（截至 20260807）与上期差分——两条元审计论断被实证

本期数字全线大幅改善，但**改善方式恰好验证了元审计的两个核心论断**，不可读成「系统变好了」。

**① 「至今」列不可作校准依据——一周动 9pp，而 10 日固定窗口列纹丝不动**

| 分级 | 至今超额 0731→0807 | 10 日均值 0731→0807 |
|---|---|---|
| A | −12.3% → **+0.0%**（+12.3pp） | +21.3% → +21.3%（**完全不变**） |
| B | −21.2% → **−11.7%**（+9.5pp） | +1.8% → +1.8%（**完全不变**） |
| C | −11.5% → −3.3%（+8.2pp） | −10.8% → −10.6% |
| 全部 | −14.5% → **−5.5%**（+9.0pp） | −3.1% → −3.0% |

10 日列 A/B 完全不动＝本周无新信号满 10 根 K 线，老样本原样。**同一批标的、同一周，
「至今」列动了 9~12pp 而固定窗口列 0pp** ⟹ 复盘 A1 修订「校准分级权重优先看 10 日列、
至今列仅作参考」得到直接实证；P23 证据①采信「至今超额」的问题因此更确凿。

**② 未执行成本可纯靠价格反弹翻号——三期符号已变**

卖类累计未执行成本：`−103,493`（0724）→ `−60,992`（0731）→ **`+9,195`（0807）**。

「拖着不卖」现在账面上是**赚的**。而同期风控卖出执行率 **33/68 分子分母都未动**。
元审计「明确不建议：不要因为未执行成本改善就放松执行纪律」在三期内被完整验证——
该指标的符号由行情决定，不由执行行为决定。

**③ 影子指标单周波动 11.2pp，是 E18 判据阈值的 2 倍多**

速通−严格闸：`−23.0pp`（0731）→ **`−11.8pp`（0807）**，样本仍是 73 条**零增长**。
E18 判据阈值是 −5pp ⟹ **一个单周能自己走 11pp 的指标，配了一个 5pp 的阈值**。
这不是新问题，是元审计问题 4「该口径是『至今』累计、无固定窗口」的必然后果，
现在有了量化尺度。**E18 v2 的必要性因此从「建议」升级为「判据在当前口径下无意义」。**

## 5.4 数据质量急剧恶化（本期最该先处理的运行时问题）

| 指标 | 0731 期 | **0807 期** |
|---|---|---|
| 近 10 交易日快照覆盖 | 6/10 | **2/10** |
| 「无法对账·缺快照」指令 | 6 条 | **14 条** |
| 快照最新日 | 20260728 | **20260728（未变）** |

缺 0729/0730/0731/0803/0804/0805/0806/0807 连续 8 天。长川科技与锐捷网络自 0728 起
每个交易日都有 sell 指令，**全部无法对账**。第三段持仓归因仍停在 20260728 快照，
而报告标题是「截至 20260807」。

根因不是 bug：#263 安全修复（本仓 public、持仓明细不可进 git）停用了 git push 入库通道，
改走 FaaS API / 前端 Holdings 页「每日快照」tab，需 admin 登录。**主线无凭据、也不应有。**
owner 0807 已提供持仓截图，数据已整理待录（见本轮对话），录入后可一次补掉 10 天缺口。

⚠️ 附带待确认：截图（A股页）无「曙26发债」，而 0728 快照有 1,000 元——转股/到期，
还是分类页未截到？影响账户总额对账。

## 5.5 本批处置的判断依据（owner 授权「按你的来」，逐条留痕）

1. **先修数据、再上首页**——四腿三腿价格落后一周，把图放上首页只会让错误更醒目。故
   `618dc6f` 排在前端工作之前推。
2. **陈旧不静默补插值**——不用最近价近似、不四舍五入掩盖，让它在计划正文里显式报警。
   **告警行本身即回归验证**：日更生效后自动消失，消失即证明修复到位。
3. **不代录持仓**——public repo + gitignore + #263 的安全设计是对的，不绕过。
4. **A 级信号截断记为 owner 明示授权**——规范要求 A 级人工复核，天禄科技/中船特气
   两只在本轮对话中获授权，留痕于 `f89bfed`。
5. **不因本期数字改善而下调任何问题的严重度**——5.3 已说明改善来自行情而非机制。
