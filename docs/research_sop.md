# 策略研究流程 SOP（owner 2026-08-02 确立）

> 适用范围：任何触及买卖规则、仓位、信号、参数的策略变更。
> 原则：**这是一流投资系统的研究流程，不是试参数。任何一步跳过，结论作废。**

## 七步流程

### 1. 命题与判据预登记（跑数之前）
- 用一句话写清命题（要改什么、为什么认为它更好）。
- **判据先写死**：过关线、评估窗口、资金口径、必报指标、稳健性要求，全部落到 `docs/model_change_proposals.md` 的 E 序号段并提交。
- 判据一旦登记，**跑数过程中不得修改**。为让某个历史事件过关而回调阈值＝p-hacking 红线。

### 2. 数据拉取与校准
- 数据来源、起止、行数、缺口全部核验并记录。
- 必做：交易日对齐、缺失/重复/异常值扫描、与独立源抽样对账。
- 数据问题的影响量级必须量化，不能只说"有个小缺口"。

### 3. 质疑数据（不是走过场）
- 问三件事：这个字段的口径是什么？它在决策时点真的可得吗（前视偏差）？它在极端行情下会不会反向失效？
- 已有教训：两融比在崩盘底反向飙升；红利价格锚因指数趋势上行而永久失效；恐慌指数是收盘后才可得。

### 4. 独立 Agent 并行设计与验证
- 至少 **2 个设计 Agent**（不同技术路线，互不知晓对方方案）+ **1 个红队 Agent**（只负责挑错：复算关键结论、审前视偏差、评估过拟合与选择性偏差、检验口径陷阱）。
- 每个 Agent 交付：可复现脚本 + 结论 Markdown + **失败方案清单**。
- Agent 必须被明确要求"如实报告达不到目标"，负结果与正结果同等对待。

### 5. 交叉核对与裁决
- 主线（我）独立跑一份基线，与 Agent 结果对账；数字对不上必须定位到口径差异或 bug，不允许含糊带过。
- 红队提出的问题逐条回应：确认 / 部分确认并修正 / 有证据地驳回。
- 对照判据逐条判定过关与否，**不满足即不上线**，负结果入库。

### 6. 图表与指标
必报（缺一不可）：
- 年化收益、年化波动、**夏普比（无风险利率 2%）**、最大回撤、卡玛比（年化/最大回撤）
- 买入/卖出次数与年均频次、平均持仓占比（现金拖累）
- 分半段（前半/后半）结果 —— 符号翻转即判不稳健
- 图：净值曲线 + 回撤带、买卖点位标注在价格图上、与基线（买入持有/定投）对照

### 7. 汇报
- 先给结论与判据判定，再给数字，最后给图。
- 明确标注置信度（强 / 中 / 弱），弱证据（如 n=1 个市场周期）必须显式说明外部效度局限。
- 明确说明"我建议做什么"和"需要 owner 决策什么"。

## 反面清单（历史教训，不得重犯）

| 教训 | 出处 |
|---|---|
| 单段最优参数在留出段全军覆没 | E21 v4 R1 |
| 多变体择优后不报选择性偏差 | E25/E29 自查 |
| 用 XIRR 口径报收益、让闲置资金不计入分母，导致预期虚高 | 2026-08-01 口径更正 |
| 只看触发次数不看触发天数占比，忽略连续触发耗尽弹药 | E30 科创50（DD40 连续 335 个交易日） |
| 按日合成多腿净值造成日历错位、产生假回撤 | E27 联合模拟 |
| 为让历史事件过关而调阈值 | 治理红线，始终禁止 |

## 口径约定（除非命题另有说明）

- 资金：一笔钱 100 元投入、无追加；闲置现金按 2%/年计息。追加口径（月度定额）作为敏感性对照，两者结论若排序翻转必须报告。
- 无风险利率：2%（夏普计算用）。
- 回测窗口：各腿以锚预热完成日为起点，统一终点为最新交易日。
- 复现脚本一律只读 dump 文件、不连生产库、不联网。

## 规格必填项增补（2026-08-03·M1 多触发器竞争）

来源：life-teachers `topics/investing.md`「仓位与容错体系」第六节。陈老师的真实决策是
**同时持有多条独立触发条件、谁先触发用谁**，而不是单规则串行执行。

> **逐条独立回测会系统性误估这类方法**——真实执行中一条规则常被另一条抢先触发并消耗掉资金/额度。

**因此，凡涉及「多条买入腿并存」或「多条卖出腿并存」的提案，验证必须同时报告两组结果：**

| 口径 | 定义 |
|---|---|
| **独立口径** | 每条腿各自独立回测，互不影响（现行做法） |
| **竞争口径** | 所有腿共享同一资金池；先触发者先消耗，后触发者只能用剩余额度 |

**两者结论不一致时以竞争口径为准。** 本仓 2026-08 前已跑的 E31/E35/E36/E37 与已登记的
E38/E39/E40/E42/E43 均为独立口径，重跑或首评时须补竞争口径。

## 判据设计前置要求（2026-08-04·E26/E38 联合首跑教训）

**凡「修复某腿跑输基准」类命题，写死判据之前必须先做可行域标定。** 本轮教训：

1. **过关线要落在可行域内**。E26/E38 的判据①（补到买入持有 −0.5pp 以内）在几乎所有保留分批纪律的
   配置下都够不着，而唯一够得着的路径又自带作弊性质。标定方法：先跑三个极点——
   ①待测部件全部拆除、②纯买入持有、③现状，可行区间就在其间。
2. **判据必须堵住作弊通道**。任何把仓位推向 100% 的方案都会自动「修复」年化差距。
   本轮 E38 判据集没堵住（D2 自扫的 F8 过①②③），靠 Agent 自觉排除。
   **均仓上限这类反作弊条款要按可行域标定后再定数值**——E26 的 85% 上限被 83.5% 合法绕过，形同虚设。
3. **敞口不是收益上界**。「把闸门全拆掉能达到 X，所以这类修法的天花板是 X」是**无效推理**——
   择时的符号可正可负，选择性开闸可以同时做到更低敞口与更高收益（本轮实测：
   全开闸 95.7% 均仓得 8.70%，选择性开闸 83.5% 均仓得 9.54%）。
   本轮 D1 与主线**各自独立犯了这个错**，均已收回。

## 起点与窗口的两条硬规矩（2026-08-04·同批教训）

1. **策略与基准必须同起点**，且起点 = **策略第一个可交易日**（锚/指标预热完成日），不是数据起点、
   更不是硬编码的年初。本轮头条「红利腿跑输 3.24pp」中约 1/3 是起点伪影：错配的 18 个交易日里
   全收益指数涨了 25.9%，基准全额吃到、策略买不了。对齐后缺口 −2.0~−2.2pp。
2. **重叠滚动窗口的统计量必须同时报有效独立样本数**。本轮「115 个滚动十年窗口、跑赢率 35%」的
   不重叠独立窗口只有 **1.95 个**，且全部胜局由「2008 崩盘是否落在窗口前段」这一单一事件决定。
   **以后此类统计必须并报：窗口数 / 不重叠独立窗口数 / 胜负是否集中于单一事件。**
3. **长短窗结论相反时必须并列报告**。同一条红利腿：19.5 年窗口跑输 2.0pp、11.2 年窗口跑赢 0.91pp。

## M1 竞争口径的两条实战补充（2026-08-04）

1. **触发次数不等于资金影响。** E9 v2 实测：接入次佳买点腿后恐慌抢买腿的触发次数
   **22 → 22 一次不少**，但**累计投入金额掉了 45%~81%**。
   **⇒ 竞争口径必须同时报「触发次数」与「累计投入/兑现金额」两组数，只看次数会得出「没影响」的错误结论。**
2. **独立口径在本系统是低估还是高估要具体看。** E26 路线上独立口径低估（−0.4~−3.1pp），
   E39 路线上竞争口径反而更差（62.5% 组合年化更低，最多 −0.42pp）。**没有固定方向，必须两组都跑。**
